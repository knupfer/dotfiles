### BEGIN_GENERIC

# i3 config file (v4)

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
font fixed
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10

# Start in command mode an update config.
exec sh ~/.i3/change-mode.sh ACTIVE; 
exec i3-msg 'rename workspace to "a"'

focus_follows_mouse no

### END_GENERIC

### BEGIN_ACTIVE
floating_minimum_size 10 x 17
# Resize windows.
mode "resize" {
        bindsym i           resize shrink width 10 px or 10 ppt
        bindsym e           resize grow height 10 px or 10 ppt
        bindsym udiaeresis           resize shrink height 10 px or 10 ppt
        bindsym a   resize grow width 10 px or 10 ppt

        bindsym Left        resize shrink width 10 px or 10 ppt
        bindsym Down        resize grow height 10 px or 10 ppt
        bindsym Up          resize shrink height 10 px or 10 ppt
        bindsym Right       resize grow width 10 px or 10 ppt

        bindsym r mode "default";
        bindsym Ctrl+i mode "default"; exec sh ~/.i3/change-mode.sh INACTIVE
}

mode "nethack" {
        bindsym --release Mod1+i exec --no-startup-id xdotool type 'h'
        bindsym --release Ctrl+Mod1+i exec --no-startup-id xdotool type 'H'
        bindsym --release Mod1+e exec --no-startup-id xdotool type 'j'
        bindsym --release Ctrl+Mod1+e exec --no-startup-id xdotool type 'J'
        bindsym --release Mod1+a exec --no-startup-id xdotool type 'l'
        bindsym --release Ctrl+Mod1+a exec --no-startup-id xdotool type 'L'
        bindsym --release Mod1+udiaeresis exec --no-startup-id xdotool type 'k'
        bindsym --release Ctrl+Mod1+udiaeresis exec --no-startup-id xdotool type 'K'
        bindsym --release Mod1+u exec --no-startup-id xdotool type 'y'
        bindsym --release Ctrl+Mod1+u exec --no-startup-id xdotool type 'Y'
        bindsym --release Mod1+odiaeresis exec --no-startup-id xdotool type '.'
        bindsym --release Mod1+period exec --no-startup-id xdotool type 'u'
        bindsym --release Ctrl+Mod1+period exec --no-startup-id xdotool type 'U'
        bindsym --release Mod1+comma exec --no-startup-id xdotool type 'n'
        bindsym --release Ctrl+Mod1+comma exec --no-startup-id xdotool type 'N'
        bindsym --release Mod1+y exec --no-startup-id xdotool type 'b'
        bindsym --release Ctrl+Mod1+y exec --no-startup-id xdotool type 'B'

        bindsym Ctrl+n mode "default"
        bindsym Ctrl+i mode "default"; exec sh ~/.i3/change-mode.sh INACTIVE
}

bindsym Ctrl+n mode "nethack"
# class                 border  backgr. text    indicator
client.focused          #4c7899 #22aabb #ffffff #aaa0dd
client.focused_inactive #333333 #555544 #ffffff #555544
client.unfocused        #333333 #555544 #ffffff #555544
client.urgent           #2f343a #900000 #ffffff #900000

# Show the windows clearly with all borders to simplify splitting.
new_window pixel 3
hide_edge_borders none

# Start a terminal.
bindsym Return exec xterm -rv -b 0 -w 0

# Start emacs.
bindsym Ctrl+e exec emacs -u root

# Deactivate command mode and change config.
bindsym Ctrl+i exec sh ~/.i3/change-mode.sh INACTIVE

# Kill focused window.
bindsym q kill

# Start dmenu (a program launcher).
bindsym Shift+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
bindsym d exec --no-startup-id i3-dmenu-desktop

# Change focus.
bindsym i focus left
bindsym e focus down
bindsym udiaeresis focus up
bindsym a focus right
bindsym Left focus left
bindsym Down focus down
bindsym Up focus up
bindsym Right focus right

# Move focused window.
bindsym Shift+i move left
bindsym Shift+e move down
bindsym Shift+udiaeresis move up
bindsym Shift+a move right
bindsym Shift+Left move left
bindsym Shift+Down move down
bindsym Shift+Up move up
bindsym Shift+Right move right

# Split in horizontal orientation.
bindsym h split h

# Split in vertical orientation.
bindsym v split v
bindsym g layout toggle split

# Enter fullscreen mode for the focused container.
bindsym f fullscreen

# Change container layout (stacked, tabbed).
bindsym s layout stacking
bindsym t layout tabbed

# Toggle tiling / floating.
bindsym Shift+space floating toggle

# Change focus between tiling / floating windows.
bindsym space focus mode_toggle

# Focus the parent container.
bindsym p focus parent

# Focus the child container.
bindsym c focus child

# Move the currently focused window to the scratchpad.
bindsym Shift+n move scratchpad

# Show the next scratchpad window or hide the focused scratchpad window.
# If there are multiple scratchpad windows, this command cycles through them.
bindsym n scratchpad show

# Switch to workspace.
bindsym Ctrl+a workspace a
bindsym Ctrl+b workspace b
bindsym Ctrl+c workspace c
bindsym Ctrl+d workspace d

# Move focused container to workspace.
bindsym Ctrl+Shift+a move container to workspace a
bindsym Ctrl+Shift+b move container to workspace b
bindsym Ctrl+Shift+c move container to workspace c
bindsym Ctrl+Shift+d move container to workspace d

bindsym r mode "resize"

# Restart i3 inplace (preserves your layout/session, can be used to upgrade i3).
bindsym Shift+r restart

# Exit i3 (logs you out of your X session)
bindsym Ctrl+Mod1+q exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

bar {
        mode dock
}

### END_ACTIVE

### BEGIN_INACTIVE

# class                 border  backgr. text    indicator
client.focused          #4c7899 #22aabb #ffffff #22aabb
client.focused_inactive #333333 #555544 #ffffff #555544
client.unfocused        #333333 #555544 #ffffff #555544
client.urgent           #2f343a #900000 #ffffff #900000

bar {
    mode invisible
}

hide_edge_borders both
new_window pixel 1
bindsym Ctrl+i exec sh ~/.i3/change-mode.sh ACTIVE
### END_INACTIVE